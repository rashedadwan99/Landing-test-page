export type SectionProps = {
  children?: React.ReactNode;
  title: string;
  coloredTitle: string;
  description: string;
  subTitle: string;
  customClass?: string;
  id: string;
  descAlignment?: string;
  headerAlignment?: string;
};
